apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starwit-accounts-internal.fullname" . }}-test-connection"
  labels:
    {{- include "starwit-accounts-internal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starwit-accounts-internal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
