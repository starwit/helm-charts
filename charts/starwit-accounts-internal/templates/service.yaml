apiVersion: v1
kind: Service
metadata:
  name: {{ include "starwit-accounts-internal.fullname" . }}
  labels:
    {{- include "starwit-accounts-internal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starwit-accounts-internal.selectorLabels" . | nindent 4 }}
