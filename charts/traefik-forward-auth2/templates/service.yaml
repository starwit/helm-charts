apiVersion: v1
kind: Service
metadata:
  name: {{ include "traefik-forward-auth2.fullname" . }}
  labels:
    {{- include "traefik-forward-auth2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traefik-forward-auth2.selectorLabels" . | nindent 4 }}
