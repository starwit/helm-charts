apiVersion: v1
kind: Service
metadata:
  name: {{ include "cityos-backend.fullname" . }}
  labels:
    {{- include "cityos-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cityos-backend.selectorLabels" . | nindent 4 }}
