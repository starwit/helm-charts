apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cityos-backend.fullname" . }}-test-connection"
  labels:
    {{- include "cityos-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cityos-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
