repositories:
- name: jetstack
  url: https://charts.jetstack.io

releases:
  - name: cert-manager
    chart: jetstack/cert-manager
    inherit:
      - template: defaultns
    values:
      - values/cert-manager-values.yaml
