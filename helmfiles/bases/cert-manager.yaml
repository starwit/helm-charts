repositories:
- name: jetstack
  url: https://charts.jetstack.io

releases:
  - name: cert-manager
    chart: jetstack/cert-manager
    inherit:
      - template: defaultns
    values:
      - ../cert-manager/values/cert-manager-values.yaml
    installed: {{ eq .Environment.Name "prod" | toYaml }}

#  - name: cert-manager-issuer
#    chart: ../../charts/cert-manager-issuer
#    inherit:
#      - template: defaultns
#    needs:
#      - cert-manager
#    installed: {{ eq .Environment.Name "prod" | toYaml }}
